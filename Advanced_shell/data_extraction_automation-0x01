#!/bin/bash

# Extract name, height, weight, and type from the JSON
name=$(jq -r '.name' data.json)
height=$(jq -r '.height' data.json)   # In decimetres
weight=$(jq -r '.weight' data.json)   # In hectograms
type=$(jq -r '.types[0].type.name' data.json)

# Convert height to meters, weight to kg
height_m=$(awk "BEGIN {printf \"%.1f\", $height / 10}")
weight_kg=$(awk "BEGIN {printf \"%.0f\", $weight / 10}")

# Format output
echo "$name is of type $type, weighs ${weight_kg}kg, and is ${height_m}m tall."
